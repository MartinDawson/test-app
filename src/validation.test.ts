
import { shouldNotBeEmpty, shouldBeNumbersAndLetters, testInputLength64 } from "./validation"
describe('empty validation', () => {


});

describe('only letters and numbers', () => {

})

test('appId must not be empty and must only be letters and numbers', () => {



})

test('projectName should not be empty and is less than 64 chars', () => {

})